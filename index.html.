import React from "react"; import { motion } from "framer-motion"; import { Button } from "@/components/ui/button"; import { Card, CardContent, CardDescription, CardHeader, CardTitle } from "@/components/ui/card"; import { Input } from "@/components/ui/input"; import { Textarea } from "@/components/ui/textarea"; import { Tabs, TabsContent, TabsList, TabsTrigger } from "@/components/ui/tabs"; import { Badge } from "@/components/ui/badge"; import { Separator } from "@/components/ui/separator"; import { Sparkles, Leaf, Heart, Star, Phone, Mail, MapPin, Calendar, Clock, ChevronRight, Check, } from "lucide-react";

// --- Utility components --- const Container = ({ children, className = "" }) => (

  <div className={`mx-auto w-full max-w-7xl px-6 ${className}`}>{children}</div>
);const Section = ({ id, title, subtitle, children }) => (

  <section id={id} className="py-16 md:py-24">
    <Container>
      <div className="mb-10 flex flex-col items-start gap-2 md:mb-14">
        {title && (
          <motion.h2
            initial={{ opacity: 0, y: 10 }}
            whileInView={{ opacity: 1, y: 0 }}
            viewport={{ once: true }}
            className="text-3xl font-semibold tracking-tight md:text-4xl"
          >
            {title}
          </motion.h2>
        )}
        {subtitle && (
          <p className="max-w-2xl text-muted-foreground">{subtitle}</p>
        )}
      </div>
      {children}
    </Container>
  </section>
);const Pill = ({ children }) => ( <span className="inline-flex items-center rounded-full border px-3 py-1 text-sm text-muted-foreground"> {children} </span> );

// --- Main Page --- export default function SoftHavenSite() { return ( <div className="min-h-screen bg-gradient-to-b from-rose-50 via-white to-emerald-50"> <Header /> <Hero /> <Highlights /> <Services /> <Memberships /> <MenuAndPricing /> <Booking /> <Testimonials /> <About /> <Contact /> <Footer /> </div> ); }

// --- Header --- function Header() { return ( <header className="sticky top-0 z-50 w-full border-b bg-white/70 backdrop-blur supports-[backdrop-filter]:bg-white/60"> <Container className="flex h-16 items-center justify-between gap-6"> <a href="#home" className="flex items-center gap-2"> <div className="grid h-9 w-9 place-items-center rounded-2xl bg-gradient-to-br from-emerald-200 to-rose-200"> <Leaf className="h-5 w-5" /> </div> <span className="text-lg font-semibold">SoftHaven</span> </a> <nav className="hidden items-center gap-6 md:flex"> {[ ["Services", "#services"], ["Memberships", "#memberships"], ["Menu", "#menu"], ["Book", "#booking"], ["About", "#about"], ["Contact", "#contact"], ].map(([label, href]) => ( <a key={label} href={href} className="text-sm text-muted-foreground hover:text-foreground"> {label} </a> ))} <a href="#booking"> <Button size="sm" className="rounded-xl"> Book Now </Button> </a> </nav> <a href="#booking" className="md:hidden"> <Button size="sm" className="rounded-xl"> Book </Button> </a> </Container> </header> ); }

// --- Hero --- function Hero() { return ( <section id="home" className="relative overflow-hidden"> <div className="pointer-events-none absolute inset-0 -z-10 opacity-60 [mask-image:radial-gradient(ellipse_at_center,black,transparent_70%)]"> <div className="absolute -top-32 left-1/2 h-96 w-[110%] -translate-x-1/2 rounded-[40px] bg-[conic-gradient(at_top,_var(--tw-gradient-stops))] from-rose-100 via-emerald-100 to-sky-100 blur-2xl" /> </div> <Container className="flex min-h-[60vh] flex-col items-center justify-center gap-8 py-16 text-center md:min-h-[70vh] md:py-24"> <motion.div initial={{ opacity: 0, y: 10 }} animate={{ opacity: 1, y: 0 }} transition={{ duration: 0.6 }}> <Pill> <Sparkles className="mr-2 h-4 w-4" /> Nurturing, luxurious, accessible </Pill> </motion.div> <motion.h1 initial={{ opacity: 0, y: 10 }} animate={{ opacity: 1, y: 0 }} transition={{ duration: 0.6, delay: 0.05 }} className="max-w-3xl text-4xl font-semibold leading-tight tracking-tight md:text-6xl" > A softer place to land. </motion.h1> <p className="max-w-2xl text-muted-foreground"> Therapeutic massage in a serene, spa-forward setting. Grounded technique, intuitive touch, and a little sparkle. </p> <div className="flex flex-wrap items-center justify-center gap-3"> <a href="#booking"> <Button size="lg" className="rounded-2xl px-8"> Reserve Your Session <ChevronRight className="ml-2 h-5 w-5" /> </Button> </a> <a href="#menu"> <Button variant="outline" size="lg" className="rounded-2xl"> View Menu </Button> </a> </div> <div className="grid w-full max-w-4xl grid-cols-2 gap-3 md:grid-cols-4"> {["Licensed LMT", "Mindful Pressure", "Luxury Touches", "Easy Rebooking"].map((t) => ( <Card key={t} className="rounded-2xl border-muted/50"> <CardContent className="flex items-center gap-2 p-4 text-sm text-muted-foreground"> <Check className="h-4 w-4" /> {t} </CardContent> </Card> ))} </div> </Container> </section> ); }

// --- Highlights --- function Highlights() { return ( <Section
id="highlights"
title="What makes SoftHaven different"
subtitle="Rooted in therapeutic skill, delivered with spa-level care."
> <div className="grid gap-6 md:grid-cols-3"> {[ { icon: <Leaf className="h-5 w-5" />, title: "Grounded & gentle", text: "Focused body mechanics and pressure control to work deeply—without strain.", }, { icon: <Heart className="h-5 w-5" />, title: "Client-centered", text: "Every session is tailored to your goals: relief, relaxation, or both.", }, { icon: <Sparkles className="h-5 w-5" />, title: "Luxury details", text: "Warm tables, aromatic notes on request, and an unhurried, caring presence.", }, ].map((card) => ( <Card key={card.title} className="rounded-2xl"> <CardHeader> <div className="mb-1 flex items-center gap-2 text-emerald-700">{card.icon}<span className="font-medium">{card.title}</span></div> <CardDescription>{card.text}</CardDescription> </CardHeader> </Card> ))} </div> </Section> ); }

// --- Services --- function Services() { const services = [ { title: "SoftHaven Signature", desc: "A flowing full-body massage with focused work where you need it most.", durations: ["50 min", "80 min"], badge: "Most Loved", }, { title: "Therapeutic Focus", desc: "Targeted work for neck, back, or hips using precise techniques.", durations: ["50 min"], }, { title: "Prenatal Comfort", desc: "Side-lying comfort and calming techniques for expecting mothers.", durations: ["50 min"], }, ];

return ( <Section id="services" title="Services" subtitle="Thoughtful offerings to meet you where you are."> <div className="grid gap-6 md:grid-cols-3"> {services.map((s) => ( <Card key={s.title} className="rounded-2xl"> <CardHeader> <div className="flex items-center justify-between"> <CardTitle className="text-xl">{s.title}</CardTitle> {s.badge && <Badge className="rounded-full">{s.badge}</Badge>} </div> <CardDescription>{s.desc}</CardDescription> </CardHeader> <CardContent className="flex flex-wrap gap-2"> {s.durations.map((d) => ( <Badge key={d} variant="secondary" className="rounded-full"> {d} </Badge> ))} </CardContent> </Card> ))} </div> </Section> ); }

// --- Memberships --- function Memberships() { return ( <Section
id="memberships"
title="Memberships"
subtitle="Commit gently to your well-being and save with monthly credits."
> <div className="grid gap-6 md:grid-cols-3"> <Card className="rounded-2xl"> <CardHeader> <CardTitle>Haven 50</CardTitle> <CardDescription>One 50-min session every month</CardDescription> </CardHeader> <CardContent className="space-y-4"> <PriceTag amount={75} note="/mo" /> <FeatureList items={["Rollover for 60 days", "Member pricing on add-ons", "Priority rebooking"]} /> <Button className="w-full rounded-xl">Join Haven 50</Button> </CardContent> </Card> <Card className="rounded-2xl border-emerald-600"> <CardHeader> <div className="flex items-center justify-between"> <CardTitle>Haven 80</CardTitle> <Badge className="rounded-full">Best Value</Badge> </div> <CardDescription>One 80-min session every month</CardDescription> </CardHeader> <CardContent className="space-y-4"> <PriceTag amount={110} note="/mo" /> <FeatureList items={["Rollover for 90 days", "+10% off upgrades", "Guest pass 1×/quarter"]} /> <Button className="w-full rounded-xl">Join Haven 80</Button> </CardContent> </Card> <Card className="rounded-2xl"> <CardHeader> <CardTitle>Shareable Duo</CardTitle> <CardDescription>Two 50-min credits to share</CardDescription> </CardHeader> <CardContent className="space-y-4"> <PriceTag amount={145} note="/mo" /> <FeatureList items={["Credits shareable with 1 guest", "Flexible scheduling", "Member add-on pricing"]} /> <Button className="w-full rounded-xl">Join Duo</Button> </CardContent> </Card> </div> </Section> ); }

function PriceTag({ amount, note }) { return ( <div className="flex items-end gap-1"> <span className="text-4xl font-semibold">${amount}</span> <span className="mb-1 text-muted-foreground">{note}</span> </div> ); }

function FeatureList({ items = [] }) { return ( <ul className="space-y-2 text-sm text-muted-foreground"> {items.map((i) => ( <li key={i} className="flex items-start gap-2"> <Check className="mt-0.5 h-4 w-4 flex-shrink-0" /> <span>{i}</span> </li> ))} </ul> ); }

// --- Menu & Pricing --- function MenuAndPricing() { const menu = [ { name: "Signature Massage (50 min)", price: 95, desc: "Full-body flow + focused work; pressure to preference.", }, { name: "Signature Massage (80 min)", price: 135, desc: "More time for detailed neck/back or full relaxation.", }, { name: "Therapeutic Focus (50 min)", price: 105, desc: "Targeted neck/shoulder or low-back/hips focus." }, { name: "Prenatal (50 min)", price: 105, desc: "Cushioned side-lying support and soothing flow." }, ];

const addOns = [ { name: "Aromatherapy Note", price: 10 }, { name: "Warm Stones (area)", price: 15 }, { name: "Scalp Ritual (10 min)", price: 20 }, { name: "Foot Focus (10 min)", price: 20 }, ];

return ( <Section id="menu" title="Menu & Pricing" subtitle="Transparent, local-friendly pricing—no surprises."> <Tabs defaultValue="sessions" className="w-full"> <TabsList className="mb-6"> <TabsTrigger value="sessions">Sessions</TabsTrigger> <TabsTrigger value="addons">Add‑ons</TabsTrigger> <TabsTrigger value="policies">Policies</TabsTrigger> </TabsList> <TabsContent value="sessions"> <div className="grid gap-4"> {menu.map((item) => ( <div key={item.name} className="flex items-start justify-between gap-6 rounded-2xl border p-4"> <div> <p className="font-medium">{item.name}</p> <p className="text-sm text-muted-foreground">{item.desc}</p> </div> <div className="text-right"> <p className="text-xl font-semibold">${item.price}</p> <a href="#booking" className="text-sm text-emerald-700 underline-offset-4 hover:underline"> Book </a> </div> </div> ))} </div> </TabsContent> <TabsContent value="addons"> <div className="grid gap-4 md:grid-cols-2"> {addOns.map((item) => ( <Card key={item.name} className="rounded-2xl"> <CardHeader> <div className="flex items-center justify-between"> <CardTitle className="text-base">{item.name}</CardTitle> <span className="text-lg font-semibold">${item.price}</span> </div> </CardHeader> </Card> ))} </div> </TabsContent> <TabsContent value="policies"> <Card className="rounded-2xl"> <CardHeader> <CardTitle className="text-base">Simple policies</CardTitle> <CardDescription> Respectful, flexible, and designed to help you keep your routine. </CardDescription> </CardHeader> <CardContent className="space-y-3 text-sm text-muted-foreground"> <p>• Please arrive 10 minutes early for your first visit.</p> <p>• 24‑hour notice for cancellations; late arrivals may shorten session time.</p> <p>• Contraindications are screened at intake; we may refer out when appropriate.</p> </CardContent> </Card> </TabsContent> </Tabs> </Section> ); }

// --- Booking --- function Booking() { return ( <Section id="booking" title="Book your session" subtitle="Pick a time and tell us where to focus—easy."> <div className="grid gap-8 md:grid-cols-2"> <Card className="rounded-2xl"> <CardHeader> <CardTitle>Quick Request</CardTitle> <CardDescription> Prefer we reach out? Send a request and we’ll confirm availability. </CardDescription> </CardHeader> <CardContent className="space-y-4"> <div className="grid gap-3 md:grid-cols-2"> <Input placeholder="Full name" /> <Input placeholder="Email" type="email" /> <Input placeholder="Phone" type="tel" /> <Input placeholder="Preferred date (MM/DD)" /> </div> <Textarea placeholder="Focus areas, pressure preference, notes" /> <Button className="w-full rounded-xl"> <Calendar className="mr-2 h-4 w-4" /> Request Booking </Button> <p className="text-xs text-muted-foreground"> *This form is a demo. Connect to your scheduler to enable live booking. </p> </CardContent> </Card> <Card className="rounded-2xl"> <CardHeader> <CardTitle>Prefer to call or email?</CardTitle> <CardDescription>We’re happy to help you choose the right session.</CardDescription> </CardHeader> <CardContent className="space-y-3 text-sm"> <a href="tel:+1-000-000-0000" className="flex items-center gap-2"> <Phone className="h-4 w-4" /> (000) 000‑0000 </a> <a href="mailto:hello@softhaven.example" className="flex items-center gap-2"> <Mail className="h-4 w-4" /> hello@softhaven.example </a> <div className="flex items-center gap-2 text-muted-foreground"> <MapPin className="h-4 w-4" /> Dallas–Fort Worth, TX </div> <div className="flex items-center gap-2 text-muted-foreground"> <Clock className="h-4 w-4" /> Wed–Sat 3–7p, Sun 10a–2p (training hours) </div> </CardContent> </Card> </div> </Section> ); }

// --- Testimonials --- function Testimonials() { const quotes = [ { name: "Relaxed & Renewed", text: "One of the best massages I’ve had—intuitive, caring, and grounded.", stars: 5, }, { name: "Focused Relief", text: "Neck and shoulder tension melted away without feeling bruised after.", stars: 5, }, { name: "Calming Presence", text: "I felt truly cared for. The slower pace and warm details are everything.", stars: 5, }, ]; return ( <Section id="testimonials" title="Client words" subtitle="Kind feedback from real sessions."> <div className="grid gap-6 md:grid-cols-3"> {quotes.map((q) => ( <Card key={q.name} className="rounded-2xl"> <CardHeader> <div className="flex items-center gap-2"> {Array.from({ length: q.stars }).map((_, i) => ( <Star key={i} className="h-4 w-4 fill-current" /> ))} </div> <CardTitle className="text-base">{q.name}</CardTitle> <CardDescription className="text-foreground">{q.text}</CardDescription> </CardHeader> </Card> ))} </div> </Section> ); }

// --- About --- function About() { return ( <Section id="about" title="About SoftHaven" subtitle="Luxury spa feel with therapeutic roots."> <div className="grid items-center gap-8 md:grid-cols-2"> <div className="space-y-4"> <p className="text-muted-foreground"> SoftHaven was created to be a peaceful, accessible space for healing and renewal. Sessions blend strong fundamentals with mindful pressure control—so you leave lighter, not sore. </p> <ul className="space-y-2 text-sm text-muted-foreground"> <li>• Licensed Massage Therapist (LMT)</li> <li>• Focus areas: neck, mid/low back, hips</li> <li>• Techniques: Swedish, therapeutic focus, prenatal</li> </ul> <div className="flex flex-wrap gap-2"> <Badge variant="secondary" className="rounded-full">Spa‑forward</Badge> <Badge variant="secondary" className="rounded-full">Therapeutic</Badge> <Badge variant="secondary" className="rounded-full">Client‑centered</Badge> </div> </div> <Card className="rounded-3xl"> <CardContent className="p-6"> <div className="rounded-2xl bg-gradient-to-br from-rose-100 via-white to-emerald-100 p-10 text-center"> <h3 className="mb-2 text-xl font-semibold">Our Intent</h3> <p className="text-muted-foreground"> To offer unhurried, attentive care that helps you breathe deeper and move easier. </p> </div> </CardContent> </Card> </div> </Section> ); }

// --- Contact --- function Contact() { return ( <Section id="contact" title="Contact" subtitle="We’re here to help with questions and scheduling."> <div className="grid gap-8 md:grid-cols-2"> <Card className="rounded-2xl"> <CardHeader> <CardTitle>Send a message</CardTitle> <CardDescription>We’ll reply within 1 business day.</CardDescription> </CardHeader> <CardContent className="space-y-4"> <Input placeholder="Your name" /> <Input placeholder="Email" type="email" /> <Textarea placeholder="How can we help?" /> <Button className="rounded-xl">Send</Button> </CardContent> </Card> <div className="space-y-4"> <div className="rounded-2xl border p-4"> <p className="mb-1 font-medium">Location</p> <p className="text-sm text-muted-foreground">Dallas–Fort Worth, TX</p> </div> <div className="rounded-2xl border p-4"> <p className="mb-1 font-medium">Hours</p> <p className="text-sm text-muted-foreground">Wed–Sat 3–7p, Sun 10a–2p</p> </div> <div className="rounded-2xl border p-4"> <p className="mb-1 font-medium">Direct</p> <div className="flex flex-col gap-1 text-sm text-muted-foreground"> <a href="tel:+1-000-000-0000" className="flex items-center gap-2"> <Phone className="h-4 w-4" /> (000) 000‑0000 </a> <a href="mailto:hello@softhaven.example" className="flex items-center gap-2"> <Mail className="h-4 w-4" /> hello@softhaven.example </a> </div> </div> </div> </div> </Section> ); }

// --- Footer --- function Footer() { return ( <footer className="border-t py-10"> <Container> <div className="flex flex-col items-center justify-between gap-6 md:flex-row"> <div className="flex items-center gap-2"> <div className="grid h-8 w-8 place-items-center rounded-xl bg-gradient-to-br from-emerald-200 to-rose-200"> <Leaf className="h-4 w-4" /> </div> <span className="font-medium">SoftHaven</span> </div> <p className="text-sm text-muted-foreground">© {new Date().getFullYear()} SoftHaven. All rights reserved.</p> <div className="text-xs text-muted-foreground">Designed with care.</div> </div> </Container> </footer> ); }
